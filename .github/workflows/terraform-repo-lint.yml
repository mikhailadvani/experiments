on:
  workflow_call: {}

jobs:
  terraform-repo-lint:
    steps:
      - uses: actions/checkout@v2

      - name: "terraform-version"
        run: |
          terraform version

      - name: "terraform-lint"
        run: |
          terraform fmt -recursive -check
